'use client'
import {useMemo,useState} from 'react'
import {Activity,AlertTriangle} from 'lucide-react'
import {useLocale,useTranslations} from 'next-intl'
import {PageHeading,StatusPill,EmptyState} from '@/components/assurance/shared'
import {changeEvents,systems} from '@/data/demo'
import {evaluateMetric} from '@/services/monitoring'
import {assuranceRepository} from '@/repositories/assurance'
import {useWorkspace} from '@/components/shell/workspace-context'
import type {MonitoringMetric,Locale} from '@/domain/models'
import {demoCopy} from '@/i18n/demo-copy'
export function Monitoring(){const t=useTranslations();const locale=useLocale() as Locale;const {organizationId}=useWorkspace();const scopedSystems=systems.filter(s=>s.organizationId===organizationId);const [current,setCurrent]=useState(()=>assuranceRepository.metrics().filter(m=>scopedSystems.some(s=>s.id===m.systemId)).map(evaluateMetric));const [baselineOnly,setBaselineOnly]=useState(false);const change=(m:MonitoringMetric,n:number)=>{const next=assuranceRepository.updateMetric(m.id,n);setCurrent(next.filter(x=>scopedSystems.some(s=>s.id===x.systemId)).map(evaluateMetric))};const events=changeEvents.filter(e=>e.organizationId===organizationId);const regressions=current.filter(x=>x.status==='REGRESSION_DETECTED');const display=useMemo(()=>current,[current]);return <><PageHeading namespace="monitoring" titleKey="title" subtitleKey="subtitle"/><div className="metric-grid"><div className="metric-card"><div className="metric-top">{t('ui.systemsMonitored')}</div><div className="metric-value">{scopedSystems.length}</div></div><div className="metric-card"><div className="metric-top">{t('ui.regressionAlerts')}</div><div className="metric-value">{regressions.length}</div></div><div className="metric-card"><div className="metric-top">{t('ui.changesDetected')}</div><div className="metric-value">{events.length}</div></div></div><section className="panel"><div className="panel-title"><div><h2>{t('monitoring.timeline')}</h2><p>{t('ui.causationCaveat')}</p></div></div>{events.length?events.map(e=><div className="timeline-row" key={e.id}><span>{e.occurredAt}</span><Activity size={16}/><span>{demoCopy(locale,'event',e.id,e.summary)}</span><StatusPill status={e.kind}/></div>):<EmptyState>{t('ui.emptyChangeEvents')}</EmptyState>}</section><section className="panel"><div className="panel-title"><div><h2>{t('ui.baselineCurrentMetrics')}</h2><p>{t('ui.thresholdInstruction')}</p></div><label><input type="checkbox" checked={baselineOnly} onChange={e=>setBaselineOnly(e.target.checked)}/> {t('ui.showBaseline')}</label></div>{display.length?<div className="table-wrap"><table><thead><tr><th>{t('ui.metricHeader')}</th><th>{t('ui.systemHeader')}</th><th>{t('monitoring.baseline')}</th><th>{t('monitoring.current')}</th><th>{t('monitoring.threshold')}</th><th>{t('monitoring.delta')}</th><th>{t('ui.statusHeader')}</th></tr></thead><tbody>{display.map(m=><tr key={m.id}><td>{demoCopy(locale,'metric',m.id,m.name)}</td><td>{scopedSystems.find(s=>s.id===m.systemId)?.name}</td><td>{m.baseline}{m.unit}</td><td>{baselineOnly?m.baseline:<input type="number" step="0.1" value={m.current} onChange={e=>change(m,Number(e.target.value))}/>}{m.unit}</td><td>{m.threshold}{m.unit}</td><td>{m.delta>0?'+':''}{m.delta}{m.unit}</td><td><StatusPill status={m.status}/></td></tr>)}</tbody></table></div>:<EmptyState>{t('ui.emptyMetrics')}</EmptyState>}{regressions.map(r=><p className="alert-inline" key={r.id}><AlertTriangle size={15}/>{t('ui.regressionDetail',{name:demoCopy(locale,'metric',r.id,r.name),baseline:r.baseline,unit:r.unit,current:r.current,threshold:r.threshold})}</p>)}</section></>}
